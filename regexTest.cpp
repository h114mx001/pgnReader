#include <bits/stdc++.h>
#include <regex.h>
using namespace std;
int main(){
    string text = R"([Event "Hello"])";
    // regex rgx (R"(\[(\w+)\s\"(\N+)\"\])");
    smatch match;
    regex rgx (R"(\[(\w+)\s\"(\N+)\"\])");
    regex_match()
}